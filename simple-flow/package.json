{
  "name": "simple-flow",
  "version": "0.1.0",
  "description": "Pi package: lightweight conversational Todoist task tracker (explicit slash commands only)",
  "type": "module",
  "keywords": ["pi-package", "pi", "pi-coding-agent", "todoist"],
  "license": "MIT",
  "pi": { "extensions": ["./extensions/simple-flow.ts"] },
  "peerDependencies": {
    "@earendil-works/pi-ai": "*",
    "@earendil-works/pi-coding-agent": "*"
  },
  "peerDependenciesMeta": {
    "@earendil-works/pi-ai": { "optional": true },
    "@earendil-works/pi-coding-agent": { "optional": true }
  },
  "scripts": { "test": "node --test" }
}
